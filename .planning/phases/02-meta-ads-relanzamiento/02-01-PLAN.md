---
phase: 02-meta-ads-relanzamiento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [ADS-02, ADS-03]
must_haves:
  truths:
    - "El token Meta está verificado y tiene permisos ads_management"
    - "El PAGE_ID de la página Facebook de MDI está identificado"
    - "Existe Custom Audience MDI_Visitantes_Landing_7d con audience_size conocido"
    - "Existe Custom Audience MDI_Checkout_Hotmart_7d (visitantes pay.hotmart.com)"
    - "Existe Custom Audience MDI_Compradores_Purchase_90d para exclusión"
    - "Los 5 videos nuevos están subidos en Meta con status ready"
    - "Los interest IDs de motos/mecánica en Argentina están identificados (mínimo 3)"
    - "El location key de CABA/Buenos Aires está verificado via API"
  artifacts:
    - path: ".planning/phases/02-meta-ads-relanzamiento/02-ASSETS.md"
      provides: "IDs de todos los assets creados: custom audiences, videos subidos, interest IDs, location key CABA"
      contains: "AUDIENCE_ID_VISITANTES, AUDIENCE_ID_CHECKOUT, AUDIENCE_ID_COMPRADORES, VIDEO_IDs subidos, PAGE_ID"
  key_links:
    - from: "Custom Audience MDI_Visitantes_Landing_7d"
      to: "Adset Warm (Plan 02)"
      via: "custom_audiences array en targeting del adset"
      pattern: "AUDIENCE_ID_VISITANTES referenciado en Plan 02 targeting"
    - from: "Custom Audience MDI_Checkout_Hotmart_7d"
      to: "Adset Hot (Plan 02)"
      via: "custom_audiences array en targeting del adset"
      pattern: "AUDIENCE_ID_CHECKOUT referenciado en Plan 02 targeting"
    - from: "Video IDs nuevos"
      to: "Ad Creatives (Plan 02)"
      via: "video_id en object_story_spec"
      pattern: "video_id en adcreatives POST payload de Plan 02"
---

<objective>
Preparar TODOS los assets de Meta Ads antes de crear la campaña: verificar token + PAGE_ID, obtener interest IDs de mecánica/motos en Argentina, verificar location key de CABA, crear las tres Custom Audiences (warm, hot, exclusión compradores), y subir los 5 videos nuevos prioritarios via API.

Purpose: Sin estos assets, el Plan 02 no puede ejecutarse — los adsets necesitan las Custom Audience IDs, los ads necesitan los video IDs, y los interest IDs no se pueden hardcodear de memoria.
Output: Archivo 02-ASSETS.md con todos los IDs listos para Plan 02 + 5 videos en estado "ready" en Meta Media Library.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/MDI_Inyeccion_Motos/.planning/STATE.md
@C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/MDI_Inyeccion_Motos/.planning/ROADMAP.md
@C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/MDI_Inyeccion_Motos/CLAUDE.md
@C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/MDI_Inyeccion_Motos/.planning/phases/02-meta-ads-relanzamiento/02-CONTEXT.md
@C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/MDI_Inyeccion_Motos/.planning/phases/02-meta-ads-relanzamiento/02-RESEARCH.md

<interfaces>
<!-- Credenciales y constantes de esta fase -->
<!-- Fuente: CLAUDE.md + STATE.md + CONTEXT.md -->

Constantes de la cuenta:
  AD_ACCOUNT_ID = act_2143097429552738
  PIXEL_ID = 2277921389363777
  TOKEN_VARIABLE = $META_TOKEN  (fuente: 05_EL_SOLAR/.env → META_ACCESS_TOKEN)
  API_BASE = https://graph.facebook.com/v19.0

Videos EXISTENTES en Meta (CTR >5.7% — NO re-subir):
  MDI_22 "Cambia pieza":              video_id 1468901108356276
  MDI_9  "Metodología diagnóstico":   video_id 1987879962608217
  MDI_21 "Menú soluciones":           video_id 1430736985403167
  MDI_10 "FOMO cupos":               video_id 943274925086396
  MDI_24 "Reto final humor":          video_id 2522742448128979

Videos NUEVOS a subir (en este orden de prioridad):
  vivo2.mp4        → título API: MDI_VIVO_02_ESCASEZ_CUPOS
  vivo1.mp4        → título API: MDI_VIVO_01_ZOOM_FACE_TO_FACE
  vivo4.mp4        → título API: MDI_VIVO_03_COMPLETO_ESCASEZ
  cambiapiezas.mp4 → título API: MDI_GANCHO_AUTORIDAD_CAMBIAPIEZAS
  +3.mp4           → título API: MDI_OFERTAS_MULTIPLE_HUMOR

URLs para Custom Audiences:
  Warm — visitantes landing:   manualdelindependiente.com (todo el dominio, pixel ID 2277921389363777)
  Hot  — checkout Hotmart:     pay.hotmart.com (URL contains)
  Compradores: Purchase event, 90 días retención
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarea 1: Verificar token + obtener PAGE_ID + interest IDs + location key CABA</name>
  <files>
    .planning/phases/02-meta-ads-relanzamiento/02-ASSETS.md
  </files>
  <read_first>
    1. `cat "C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/.env"` — obtener META_ACCESS_TOKEN
    2. `cat "C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/MDI_Inyeccion_Motos/CLAUDE.md"` — confirmar video IDs existentes
    3. `cat "C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/MDI_Inyeccion_Motos/.planning/STATE.md"` — confirmar token expiry (~10 mayo)
  </read_first>
  <action>
Ejecutar en bash los siguientes comandos en orden. Capturar y documentar TODOS los valores en 02-ASSETS.md al final.

PASO 0 — Cargar token como variable:
```bash
source "C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/.env"
META_TOKEN="$META_ACCESS_TOKEN"
```

PASO 1 — Verificar token y permisos de la cuenta:
```bash
curl -s -G \
  "https://graph.facebook.com/v19.0/act_2143097429552738" \
  --data-urlencode "fields=name,account_status,currency" \
  -H "Authorization: Bearer $META_TOKEN"
```
Resultado esperado: `"name": "...", "account_status": 1, "currency": "USD"` (o ARS).
Si hay error 190 (OAuthException): el token expiró — renovar en developers.facebook.com antes de continuar.
Si hay error 100 (Invalid OAuth access token): mismo diagnóstico.

PASO 2 — Obtener PAGE_ID de la página Facebook de MDI:
```bash
curl -s -G \
  "https://graph.facebook.com/v19.0/me/accounts" \
  --data-urlencode "fields=id,name,category" \
  -H "Authorization: Bearer $META_TOKEN"
```
Identificar la página de Manual del Independiente (MDI) en la lista. Guardar su `id` como PAGE_ID.

PASO 3 — Buscar interest IDs (ejecutar los 6 queries, capturar los mejores):
```bash
# Query 1: motocicletas
curl -s -G \
  "https://graph.facebook.com/v19.0/search" \
  --data-urlencode "type=adinterest" \
  --data-urlencode "q=motocicletas" \
  --data-urlencode "limit=5" \
  -H "Authorization: Bearer $META_TOKEN"

# Query 2: mecanica automotriz
curl -s -G \
  "https://graph.facebook.com/v19.0/search" \
  --data-urlencode "type=adinterest" \
  --data-urlencode "q=mecanica automotriz" \
  --data-urlencode "limit=5" \
  -H "Authorization: Bearer $META_TOKEN"

# Query 3: reparacion de motos
curl -s -G \
  "https://graph.facebook.com/v19.0/search" \
  --data-urlencode "type=adinterest" \
  --data-urlencode "q=reparacion de motos" \
  --data-urlencode "limit=5" \
  -H "Authorization: Bearer $META_TOKEN"

# Query 4: taller mecanico
curl -s -G \
  "https://graph.facebook.com/v19.0/search" \
  --data-urlencode "type=adinterest" \
  --data-urlencode "q=taller mecanico" \
  --data-urlencode "limit=5" \
  -H "Authorization: Bearer $META_TOKEN"

# Query 5: motorcycles (inglés — mayor cobertura en AR)
curl -s -G \
  "https://graph.facebook.com/v19.0/search" \
  --data-urlencode "type=adinterest" \
  --data-urlencode "q=motorcycles" \
  --data-urlencode "limit=5" \
  -H "Authorization: Bearer $META_TOKEN"

# Query 6: inyeccion electronica
curl -s -G \
  "https://graph.facebook.com/v19.0/search" \
  --data-urlencode "type=adinterest" \
  --data-urlencode "q=inyeccion electronica" \
  --data-urlencode "limit=5" \
  -H "Authorization: Bearer $META_TOKEN"
```
Seleccionar los 4-6 interests con audience_size_upper_bound más alto Y relevantes al mecánico argentino (excluir intereses de autos/cars que no sean motos). Guardar sus `id` y `name`.

PASO 4 — Verificar location key de CABA/Buenos Aires:
```bash
curl -s -G \
  "https://graph.facebook.com/v19.0/search" \
  --data-urlencode "type=adgeolocation" \
  --data-urlencode "q=Buenos Aires" \
  --data-urlencode "country_code=AR" \
  --data-urlencode "location_types[]=city" \
  -H "Authorization: Bearer $META_TOKEN"
```
Buscar el resultado con `name: "Buenos Aires"` y `region: "Buenos Aires"` (capital, no provincia). Guardar su `key`. Si hay múltiples resultados "Buenos Aires", seleccionar el que corresponde a la Ciudad Autónoma de Buenos Aires (CABA), no a la provincia.

PASO 5 — Escribir archivo 02-ASSETS.md con todos los valores encontrados. El archivo debe contener secciones claras:
- Token status (válido/inválido, expiry)
- PAGE_ID de MDI
- Interest IDs seleccionados (id + name + audience_size)
- Location key CABA
- (Videos y Custom Audiences se completan en Tareas 2 y 3)
  </action>
  <verify>
    <automated>
```bash
# Verificar que el archivo 02-ASSETS.md existe y contiene los valores críticos
grep -c "PAGE_ID\|Interest ID\|CABA\|location_key" "C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/MDI_Inyeccion_Motos/.planning/phases/02-meta-ads-relanzamiento/02-ASSETS.md"
# Debe devolver >= 4 (al menos una línea por cada sección)
```
    </automated>
  </verify>
  <done>
- Token Meta verificado: cuenta activa (account_status=1) con permisos de lectura/escritura a la ad account
- PAGE_ID identificado en /me/accounts
- Mínimo 4 interest IDs de motos/mecánica en Argentina con audience_size_upper_bound documentado
- Location key de CABA verificado via API (no asumido)
- Todos los valores guardados en 02-ASSETS.md sección "Token + Setup"
  </done>
</task>

<task type="auto">
  <name>Tarea 2: Crear tres Custom Audiences (warm, hot, compradores)</name>
  <files>
    .planning/phases/02-meta-ads-relanzamiento/02-ASSETS.md
  </files>
  <read_first>
    1. `cat "C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/MDI_Inyeccion_Motos/.planning/phases/02-meta-ads-relanzamiento/02-ASSETS.md"` — confirmar que el token está verificado (Tarea 1 completa)
    2. `cat "C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/.env"` — cargar META_ACCESS_TOKEN
  </read_first>
  <action>
Crear las tres Custom Audiences necesarias para los adsets warm y hot. Ejecutar en orden, guardando cada AUDIENCE_ID devuelto.

PASO 1 — Crear audiencia WARM: visitantes de la landing en los últimos 7 días:
```bash
source "C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/.env"
META_TOKEN="$META_ACCESS_TOKEN"

curl -s -X POST \
  "https://graph.facebook.com/v19.0/act_2143097429552738/customaudiences" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $META_TOKEN" \
  -d '{
    "name": "MDI_Visitantes_Landing_7d",
    "subtype": "WEBSITE",
    "description": "Visitantes manualdelindependiente.com ultimos 7 dias",
    "rule": {
      "inclusions": {
        "operator": "or",
        "rules": [
          {
            "event_sources": [
              {
                "id": "2277921389363777",
                "type": "pixel"
              }
            ],
            "retention_seconds": 604800,
            "filter": {
              "operator": "and",
              "filters": []
            }
          }
        ]
      }
    },
    "prefill": 1
  }'
```
Guardar el `id` devuelto como AUDIENCE_ID_VISITANTES_7D.

NOTA: Si la audiencia de 7 días regresa con < 100 personas (verificar en Meta Audiences UI), ejecutar la variante con `retention_seconds: 1209600` (14 días) y renombrar `MDI_Visitantes_Landing_14d`. Documentar la decisión en 02-ASSETS.md.

PASO 2 — Crear audiencia HOT: visitantes del checkout de Hotmart (pay.hotmart.com) últimos 7 días:
```bash
curl -s -X POST \
  "https://graph.facebook.com/v19.0/act_2143097429552738/customaudiences" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $META_TOKEN" \
  -d '{
    "name": "MDI_Checkout_Hotmart_7d",
    "subtype": "WEBSITE",
    "description": "Visitantes pay.hotmart.com ultimos 7 dias - no completaron Purchase",
    "rule": {
      "inclusions": {
        "operator": "or",
        "rules": [
          {
            "event_sources": [
              {
                "id": "2277921389363777",
                "type": "pixel"
              }
            ],
            "retention_seconds": 604800,
            "filter": {
              "operator": "and",
              "filters": [
                {
                  "field": "url",
                  "operator": "i_contains",
                  "value": "pay.hotmart.com"
                }
              ]
            }
          }
        ]
      }
    },
    "prefill": 1
  }'
```
Guardar el `id` devuelto como AUDIENCE_ID_CHECKOUT_7D.

NOTA: El pixel de MDI trackea PageView en manualdelindependiente.com. El pixel NO está instalado directamente en pay.hotmart.com. La regla de URL filter con `i_contains: "pay.hotmart.com"` requiere que el pixel haya visto esa URL en el browser del usuario — esto puede ocurrir via referrer si el pixel está configurado para capturar navegación post-click, pero puede resultar en una audiencia vacía si el pixel no tiene cobertura en Hotmart. Documentar en 02-ASSETS.md: si `approximate_count` de esta audiencia = 0 al verificar, el adset hot deberá usarse sin esta exclusión y simplemente dirigir a todos los visitantes de la landing (AUDIENCE_ID_VISITANTES_7D) como fallback.

PASO 3 — Crear audiencia EXCLUSIÓN: compradores (Purchase event) últimos 90 días:
```bash
curl -s -X POST \
  "https://graph.facebook.com/v19.0/act_2143097429552738/customaudiences" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $META_TOKEN" \
  -d '{
    "name": "MDI_Compradores_Purchase_90d",
    "subtype": "WEBSITE",
    "description": "Usuarios que dispararon evento Purchase en pixel MDI - excluir de todos los adsets",
    "rule": {
      "inclusions": {
        "operator": "or",
        "rules": [
          {
            "event_sources": [
              {
                "id": "2277921389363777",
                "type": "pixel"
              }
            ],
            "retention_seconds": 7776000,
            "filter": {
              "operator": "and",
              "filters": [
                {
                  "field": "event",
                  "operator": "=",
                  "value": "Purchase"
                }
              ]
            }
          }
        ]
      }
    },
    "prefill": 1
  }'
```
Guardar el `id` devuelto como AUDIENCE_ID_COMPRADORES.

PASO 4 — Verificar que las tres audiencias existen en la cuenta:
```bash
curl -s -G \
  "https://graph.facebook.com/v19.0/act_2143097429552738/customaudiences" \
  --data-urlencode "fields=id,name,approximate_count,subtype" \
  -H "Authorization: Bearer $META_TOKEN" | grep -A2 "MDI_"
```

PASO 5 — Actualizar 02-ASSETS.md con sección "Custom Audiences":
```
## Custom Audiences
| Audiencia | ID | approximate_count | Uso |
|-----------|-----|-------------------|-----|
| MDI_Visitantes_Landing_7d | [ID] | [N] | adset warm — incluir |
| MDI_Checkout_Hotmart_7d | [ID] | [N] | adset hot — incluir (o fallback si = 0) |
| MDI_Compradores_Purchase_90d | [ID] | [N] | todos los adsets — excluir |
```
  </action>
  <verify>
    <automated>
```bash
source "C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/.env"
META_TOKEN="$META_ACCESS_TOKEN"
# Verificar que las 3 audiencias existen en la cuenta
curl -s -G \
  "https://graph.facebook.com/v19.0/act_2143097429552738/customaudiences" \
  --data-urlencode "fields=id,name,subtype" \
  -H "Authorization: Bearer $META_TOKEN" | grep -o '"name":"MDI_[^"]*"' | wc -l
# Debe devolver >= 3
```
    </automated>
  </verify>
  <done>
- 3 Custom Audiences creadas: MDI_Visitantes_Landing_7d, MDI_Checkout_Hotmart_7d, MDI_Compradores_Purchase_90d
- Los tres AUDIENCE_IDs guardados en 02-ASSETS.md
- approximate_count documentado para cada una (incluyendo nota si MDI_Checkout_Hotmart_7d = 0)
- GET /customaudiences confirma las 3 audiencias presentes en la cuenta
  </done>
</task>

<task type="auto">
  <name>Tarea 3: Subir 5 videos nuevos via API y verificar status ready</name>
  <files>
    .planning/phases/02-meta-ads-relanzamiento/02-ASSETS.md
  </files>
  <read_first>
    1. `cat "C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/MDI_Inyeccion_Motos/.planning/phases/02-meta-ads-relanzamiento/02-ASSETS.md"` — confirmar que Tareas 1 y 2 están completas
    2. `cat "C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/.env"` — cargar META_ACCESS_TOKEN
    3. Confirmar con el usuario la ruta exacta donde están guardados los archivos MP4 localmente (los archivos los tiene Miguel — verificar disponibilidad ANTES de ejecutar este task). Si no están disponibles localmente, esta tarea queda BLOQUEADA: documentar en 02-ASSETS.md y proceder al Plan 02 usando solo los videos existentes en Meta.
  </read_first>
  <action>
Subir los 5 videos nuevos via API en orden de prioridad. Los videos con captions en post-producción se suben via API; los que no tienen captions se suben por UI de Meta Ads Manager para aprovechar el auto-generate de captions (ver RESEARCH.md, sección Captions).

IMPORTANTE — Captions: Según CONTEXT.md, los 5 videos nuevos NO tienen subtítulos en post-producción. La recomendación del RESEARCH.md es subirlos desde Meta Ads Manager UI para activar "Generate Automatically" al crear el ad creative. Sin embargo, el upload via API es necesario para obtener los video_ids antes de Plan 02. Solución: subir via API ahora para obtener IDs, luego al crear los ad creatives en Plan 02 usar la UI para asociar captions. Los IDs obtenidos aquí son reutilizables en la UI.

ALTERNATIVA: Si los videos no están disponibles en la máquina local donde se ejecuta este plan (están en el equipo de Miguel), documentar los IDs como PENDIENTE en 02-ASSETS.md y pasar a Plan 02 — los 5 videos se pueden subir via Ads Manager UI y los IDs se obtienen después desde la Media Library.

Si los archivos SÍ están disponibles, ejecutar para cada video:

```bash
source "C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/.env"
META_TOKEN="$META_ACCESS_TOKEN"

# Video 1: vivo2.mp4 → MDI_VIVO_02_ESCASEZ_CUPOS
curl -s -X POST \
  "https://graph.facebook.com/v19.0/act_2143097429552738/advideos" \
  -F "source=@/RUTA_LOCAL/vivo2.mp4" \
  -F "title=MDI_VIVO_02_ESCASEZ_CUPOS" \
  -H "Authorization: Bearer $META_TOKEN"
# Guardar "id" devuelto como VIDEO_ID_VIVO2

# Verificar que terminó de procesar (esperar hasta que video_status = "ready"):
# Reemplazar VIDEO_ID con el id recibido
curl -s -G \
  "https://graph.facebook.com/v19.0/VIDEO_ID" \
  --data-urlencode "fields=id,title,status" \
  -H "Authorization: Bearer $META_TOKEN"
# Repetir hasta: "video_status": "ready" (puede tardar 2-10 min)

# Video 2: vivo1.mp4 → MDI_VIVO_01_ZOOM_FACE_TO_FACE
curl -s -X POST \
  "https://graph.facebook.com/v19.0/act_2143097429552738/advideos" \
  -F "source=@/RUTA_LOCAL/vivo1.mp4" \
  -F "title=MDI_VIVO_01_ZOOM_FACE_TO_FACE" \
  -H "Authorization: Bearer $META_TOKEN"

# Video 3: vivo4.mp4 → MDI_VIVO_03_COMPLETO_ESCASEZ
curl -s -X POST \
  "https://graph.facebook.com/v19.0/act_2143097429552738/advideos" \
  -F "source=@/RUTA_LOCAL/vivo4.mp4" \
  -F "title=MDI_VIVO_03_COMPLETO_ESCASEZ" \
  -H "Authorization: Bearer $META_TOKEN"

# Video 4: cambiapiezas.mp4 → MDI_GANCHO_AUTORIDAD_CAMBIAPIEZAS
curl -s -X POST \
  "https://graph.facebook.com/v19.0/act_2143097429552738/advideos" \
  -F "source=@/RUTA_LOCAL/cambiapiezas.mp4" \
  -F "title=MDI_GANCHO_AUTORIDAD_CAMBIAPIEZAS" \
  -H "Authorization: Bearer $META_TOKEN"

# Video 5: +3.mp4 → MDI_OFERTAS_MULTIPLE_HUMOR
curl -s -X POST \
  "https://graph.facebook.com/v19.0/act_2143097429552738/advideos" \
  -F "source=@/RUTA_LOCAL/+3.mp4" \
  -F "title=MDI_OFERTAS_MULTIPLE_HUMOR" \
  -H "Authorization: Bearer $META_TOKEN"
```

NOTA sobre el nombre "+3.mp4": el símbolo + puede causar problemas en bash. Si falla, intentar con comillas: `source=@'/RUTA_LOCAL/+3.mp4'` o renombrar el archivo localmente a `mas3.mp4` antes de subir.

Verificar status de TODOS los videos antes de finalizar la tarea:
```bash
# Para cada VIDEO_ID obtenido:
curl -s -G \
  "https://graph.facebook.com/v19.0/VIDEO_ID" \
  --data-urlencode "fields=id,title,status" \
  -H "Authorization: Bearer $META_TOKEN"
# Esperado: "video_status": "ready"
# Si devuelve "processing": esperar 5 min y verificar de nuevo
```

Actualizar 02-ASSETS.md con sección "Videos Nuevos Subidos":
```
## Videos Nuevos Subidos
| Título | Video ID | Status | Adset destino |
|--------|----------|--------|---------------|
| MDI_VIVO_02_ESCASEZ_CUPOS | [ID o PENDIENTE] | ready / pendiente | Warm |
| MDI_VIVO_01_ZOOM_FACE_TO_FACE | [ID o PENDIENTE] | ready / pendiente | Warm |
| MDI_VIVO_03_COMPLETO_ESCASEZ | [ID o PENDIENTE] | ready / pendiente | Warm |
| MDI_GANCHO_AUTORIDAD_CAMBIAPIEZAS | [ID o PENDIENTE] | ready / pendiente | Cold |
| MDI_OFERTAS_MULTIPLE_HUMOR | [ID o PENDIENTE] | ready / pendiente | Hot |

NOTA CAPTIONS: Estos videos NO tienen subtítulos. Al crear ad creatives en Plan 02,
usar Meta Ads Manager UI → Video Captions → Generate Automatically para cada uno.
```
  </action>
  <verify>
    <automated>
```bash
# Verificar que 02-ASSETS.md tiene la sección de videos completada
grep -c "MDI_VIVO\|MDI_GANCHO\|MDI_OFERTAS" "C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/MDI_Inyeccion_Motos/.planning/phases/02-meta-ads-relanzamiento/02-ASSETS.md"
# Debe devolver >= 5 (una línea por video)
```
    </automated>
  </verify>
  <done>
- Los 5 videos subidos en Meta o documentados como PENDIENTE (si no hay acceso local a los MP4)
- Todos los video_ids en status "ready" documentados en 02-ASSETS.md
- Si algún video está pendiente: nota clara sobre cómo obtener el ID desde Ads Manager UI Media Library antes de Plan 02
- 02-ASSETS.md completo con: PAGE_ID, interest IDs, location key CABA, 3 Custom Audience IDs, 5 video IDs (o PENDIENTE)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell → Meta API | Las llamadas curl incluyen el token Bearer — el token no debe aparecer en logs ni en archivos del repo |
| Archivos MP4 locales → Meta API | Los videos se suben desde equipo local vía HTTPS — riesgo de exponer ruta local en logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | META_ACCESS_TOKEN en bash history | mitigate | Usar `source .env` en vez de hardcodear el token. Nunca incluir el token en 02-ASSETS.md ni en ningún archivo del repo. El .env está en .gitignore de 05_EL_SOLAR. |
| T-02-02 | Information Disclosure | 02-ASSETS.md con IDs de cuentas Meta | accept | Los IDs de campañas/audiencias Meta son identificadores, no credenciales. Bajo riesgo en repo privado. |
| T-02-03 | Tampering | Upload de video incorrecto con título incorrecto | mitigate | Verificar `title` en GET /VIDEO_ID después de subir. Si el título es incorrecto, la biblioteca de medios de Meta lo muestra — corregir antes de Plan 02. |
| T-02-04 | Denial of Service | Rate limiting de Meta API (>200 llamadas/hora) | accept | Este plan hace <20 llamadas API. Rate limit de Meta es 200 llamadas por hora por app — sin riesgo. |
</threat_model>

<verification>
Al finalizar Wave 1 (Plan 01 completo), verificar:

1. Token activo:
```bash
source "C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/.env"
curl -s -G "https://graph.facebook.com/v19.0/act_2143097429552738?fields=name,account_status" -H "Authorization: Bearer $META_ACCESS_TOKEN"
# Esperar: account_status: 1
```

2. Custom Audiences presentes (mínimo 3):
```bash
curl -s -G "https://graph.facebook.com/v19.0/act_2143097429552738/customaudiences" --data-urlencode "fields=id,name" -H "Authorization: Bearer $META_ACCESS_TOKEN" | grep "MDI_"
```

3. Archivo 02-ASSETS.md contiene todos los IDs:
```bash
grep -E "AUDIENCE_ID|PAGE_ID|Interest ID|location_key|VIDEO_ID" "C:/Users/USUARIO/Documents/Claude Code/05_EL_SOLAR/MDI_Inyeccion_Motos/.planning/phases/02-meta-ads-relanzamiento/02-ASSETS.md"
```
</verification>

<success_criteria>
- Token Meta verificado con acceso a act_2143097429552738 (account_status=1)
- PAGE_ID de la página Facebook de MDI identificado y guardado en 02-ASSETS.md
- Mínimo 4 interest IDs de mecánica/motos en Argentina documentados con audience_size_upper_bound
- Location key de CABA/Buenos Aires verificado via API adgeolocation (no asumido)
- 3 Custom Audiences creadas en la cuenta: visitantes 7d, checkout Hotmart 7d, compradores 90d
- 5 videos nuevos subidos con status "ready" (o estado PENDIENTE documentado con instrucción de fallback)
- 02-ASSETS.md completo y listo para que Plan 02 lo lea como fuente de verdad
</success_criteria>

<output>
Después de completar, crear `.planning/phases/02-meta-ads-relanzamiento/02-01-SUMMARY.md` con:
- Status de token
- PAGE_ID obtenido
- Lista de interest IDs seleccionados (id, name, audience_size)
- Location key CABA confirmado
- IDs de las 3 Custom Audiences creadas
- Status de cada video (subido/ready/pendiente)
- Cualquier bloqueante encontrado (ej. videos MP4 no disponibles localmente)
</output>
